//
// Created by Andrew Bartow on 1/29/17.
//

#ifndef OPENBADGE_CALC_UTILS_H
#define OPENBADGE_CALC_UTILS_H

#define MIN(A, B)                      (((A) < (B)) ? (A) : (B))
#define MAX(A, B)                      (((A) > (B)) ? (A) : (B))
#define CEIL(X, DIVISOR)               (((X) + (DIVISOR) - 1) / (DIVISOR))

#endif //OPENBADGE_CALC_UTILS_H
